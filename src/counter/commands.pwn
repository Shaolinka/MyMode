/* cmd:blabla(playerid, params[])
{
    extract params -> new counter_id; else return 0;

    new minute, 
        second;

    return printf("%d", GetCounterTimeToEnd(counter_id, minute, second));
} */